android {

    buildTypes {
        release {
            buildConfigField "boolean", "PROXY", "false"
        }
        debug {
            buildConfigField "boolean", "PROXY", "false"
        }
    }
}

configurations {
    debugCompile
    releaseCompile
}